[pytest]
markers =
    llm: test makes a real call to the local Ollama model (slow, requires Ollama running with phi3.5 pulled)
    e2e: real browser test via Playwright against a live subprocess server (slow, requires `playwright install chromium`)
testpaths = tests
